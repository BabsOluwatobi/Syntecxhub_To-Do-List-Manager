# Syntecxhub_To-Do-List-Manager
A menu-driven command-line To-Do List application that allows a user to:

Add tasks
View tasks
Mark tasks as completed
Delete tasks
Save tasks to a file (so they remain after restart)
